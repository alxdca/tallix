# Test environment configuration
# Uses safe application role to properly test RLS policies

# Test database connection with safe role
DATABASE_URL=postgresql://tallix_app:tallix_app_secret@localhost:5432/tallix

# Allow safe role (it doesn't have BYPASSRLS)
ALLOW_UNSAFE_DB_ROLE=false

# Server configuration
PORT=3001

# Auth
JWT_SECRET=test-secret

# Logging
LOG_LEVEL=info
NODE_ENV=test
